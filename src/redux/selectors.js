export const getContacts = state => state.contacts;

export const getFilter = state => state.filter;

export const getToken = state => state.auth.access_token;

export const getProfileInfo = state => state.auth.profile;
